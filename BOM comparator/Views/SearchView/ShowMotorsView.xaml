<UserControl x:Class="BOMComparator.Views.ShowMotorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BOMComparator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="450" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <ContentControl Grid.Row="0" VerticalContentAlignment="Center" x:Name="Filter" Margin="0 10 0 10"/>

        <Button x:Name="ExecuteSearch" Content="SEARCH" Margin="3"
            HorizontalAlignment = "Stretch" VerticalAlignment = "Stretch" 
            Grid.Row="1" IsDefault="True"/>
    </Grid>
</UserControl>
